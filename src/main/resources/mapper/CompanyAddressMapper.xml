<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="tw.com.rex.leave_system.mapper.CompanyAddressMapper">
    <resultMap id="BaseResultMap" type="tw.com.rex.leave_system.model.dao.part.company.CompanyAddress">
        <id column="company_id" jdbcType="VARCHAR" property="company.id"/>
        <result column="zip" jdbcType="VARCHAR" property="zip"/>
        <result column="address" jdbcType="VARCHAR" property="address"/>
        <association property="company" resultMap="tw.com.rex.leave_system.mapper.CompanyMapper.BaseResultMap"/>
    </resultMap>
    <delete id="deleteByPrimaryKey" parameterType="java.lang.String">
        delete
        from company_address
        where id = #{id,jdbcType=VARCHAR}
    </delete>
    <insert id="insertSelective" parameterType="tw.com.rex.leave_system.model.dao.part.company.CompanyAddress">
        <selectKey keyProperty="id" keyColumn="id" order="BEFORE" resultType="string">
            SELECT UUID() FROM dual
        </selectKey>
        insert into company_address
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="id != null">id,</if>
            <if test="zip != null">zip,</if>
            <if test="address != null">address,</if>
            <if test="company != null and company.id != null">company_id,</if>
        </trim>
        VALUES
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="id != null">#{id,jdbcType=VARCHAR},</if>
            <if test="zip != null">#{zip,jdbcType=VARCHAR},</if>
            <if test="address != null">#{address,jdbcType=VARCHAR},</if>
            <if test="company != null and company.id != null">#{company.id,jdbcType=VARCHAR},</if>
        </trim>
    </insert>
    <update id="updateSelectiveByPrimaryKey"
            parameterType="tw.com.rex.leave_system.model.dao.part.company.CompanyAddress">
        update company_address
        <set>
            <if test="zip != null">zip = #{zip,jdbcType=VARCHAR},</if>
            <if test="address != null">address = #{zip,jdbcType=VARCHAR},</if>
            <if test="company != null and company.id != null">company_id = #{company.id,jdbcType=VARCHAR},</if>
        </set>
        <where>
            id = #{id,jdbcType=VARCHAR}
        </where>
    </update>
    <select id="selectByPrimaryKey" parameterType="java.lang.String" resultMap="BaseResultMap">
        select *
        from company_address
        where company_id = #{companyId,jdbcType=VARCHAR}
    </select>
    <select id="selectAll" resultMap="BaseResultMap">
        select *
        from company_address
    </select>
</mapper>